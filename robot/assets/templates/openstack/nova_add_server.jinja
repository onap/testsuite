{
    "server": {
        "name": "{{name}}",
        "imageRef": "{{imageRef}}",
        "flavorRef": "{{flavorRef}}",
        "config_drive": true,
        "OS-DCF:diskConfig": "MANUAL",
        "metadata": {
            "{{name}}": "Server for attaching VVG"
        },
        "networks": [
            {
                "uuid": "{{public_net_id}}"
            }
        ]
    }
}